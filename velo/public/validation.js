/*
 * validation.js — shared quote-form validation helpers   [SCAFFOLD / STUB]
 *
 * STATUS: Not implemented. Used by both pages/quote.page.js (client) and
 * backend/quoteRequests.jsw (server) so validation rules live in one place.
 * Finalize after IA + plan review.
 */

// TODO(velo): implement and export real validators, e.g.:
// export function isValidEmail(v) { ... }
// export function validateQuotePayload(payload) { return { valid, errors }; }

export function validateQuotePayload(/* payload */) {
  // TODO(velo): return { valid: false, errors: { ... } } until implemented
  throw new Error('validateQuotePayload not implemented — scaffold only');
}
